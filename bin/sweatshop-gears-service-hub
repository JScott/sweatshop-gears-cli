#!/usr/bin/env ruby
require 'erubis'
require 'robot_sweatshop/config'
require 'robot_sweatshop/gears/service_hub'
require 'sinatra'

configure do
  set :port, ARGV[0] || 34871
  set :bind, configatron.http_bind
end

get '/' do
  eruby = Erubis::Eruby.new frontpage_template
  context = {}
  return eruby.result(context)
end
