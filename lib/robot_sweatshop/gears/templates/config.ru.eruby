require 'rack/reverse_proxy'

use Rack::ReverseProxy do
  reverse_proxy /^\/$/, 'http://localhost:34871'
  reverse_proxy /^\/reporter\/?(.*)$/, 'http://localhost:8082/$1'
end

app = proc do |env|
  [ 200, {'Content-Type' => 'text/plain'} ]
end
run app

# rackup -p 9000 config.ru
